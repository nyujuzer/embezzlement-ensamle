import createTheme from "@mui/material/styles/createTheme"

const theme = createTheme({
    palette:{
        primary:{
            main: "#000000",
            contrastText: "#ffffff"
        }
    }
})

export default theme